using UnityEngine;
using System.Collections;

public class ColourScheme : MonoBehaviour {

    public Color[] colourScheme = new Color[6];

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
